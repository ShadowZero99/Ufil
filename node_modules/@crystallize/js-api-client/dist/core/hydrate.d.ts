import { ClientInterface } from './client';
export type ProductHydrater = (items: string[], language: string, extraQuery?: any, perProduct?: (item: string, index: number) => any, perVariant?: (item: string, index: number) => any) => Promise<any>;
export declare function createProductHydrater(client: ClientInterface): {
    byPaths: ProductHydrater;
    bySkus: ProductHydrater;
};
