import { GridCell, GridDimensions } from './types';
export declare const getGridDimensions: (rows: any[]) => GridDimensions;
export declare function getPositionnableCellClassNames(cell: GridCell, { rows, cols }: GridDimensions): string;
export declare function getPositionnablRowClassNames({ rowIndex }: {
    rowIndex: number;
}, { rows }: GridDimensions): string;
